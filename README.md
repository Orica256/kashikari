# かしかり帳

iPhoneのホーム画面から使える貸し借り管理アプリ（PWA）です。  
App Storeへの登録不要・完全無料・オフライン対応。

## インストール方法

1. iPhoneのSafariで以下のURLを開く  
   **https://orica256.github.io/kashikari/**
2. 画面下の共有ボタン（□↑）をタップ
3. 「ホーム画面に追加」をタップ
4. 「追加」をタップ → ホーム画面にアイコンが作成される

## 機能一覧

- **ホーム** - 貸した合計・借りた合計・差引残高のサマリーと最近の記録を表示
- **人物管理** - 貸し借りする相手を登録・編集・削除・検索
- **取引履歴** - 全取引をフィルター（貸した／借りた／未返済／返済済み）と検索で絞り込み
- **取引詳細** - タイトル・説明・金額・日付・返済期限の管理
- **返済記録** - 一部返済・全額返済の記録
- **設定** - データのエクスポート・インポート・全削除
- **オフライン対応** - インターネットなしでも使用可能（データはiPhone内に保存）

## 技術情報

| 項目 | 内容 |
|------|------|
| 種別 | PWA（Progressive Web App） |
| ホスティング | GitHub Pages |
| データ保存 | localStorage（端末内） |
| 対応ブラウザ | Safari（iOS推奨） |
| 外部依存 | なし |

## ファイル構成

```
kashikari/
├── index.html    # メインHTML
├── app.js        # アプリロジック
├── style.css     # スタイル
├── sw.js         # Service Worker（オフライン対応）
├── manifest.json # PWA設定
└── icon.png      # アプリアイコン
```

## 注意事項

- データはSafariのlocalStorageに保存されます
- iPhoneを初期化するとデータが消えます
- 複数端末間のデータ同期には対応していません
- GitHubのコードを更新してもデータは消えません

---

## 変更ログ

### v4 - 設定・インポート・エクスポート追加
- ⚙️ 設定タブを追加
- JSONエクスポート機能を追加（バックアップ用）
- CSVエクスポート機能を追加（Excel用・文字化け対策済み）
- JSONインポート機能を追加（重複データは自動スキップ）
- 全データ削除機能を追加（誤操作防止のため確認ダイアログ2回）
- Service Workerキャッシュをv3に更新

### v3 - 入力・検索のバグ修正
- 入力欄タップ時にiOSが自動ズームする問題を修正（フォントサイズを16pxに統一）
- 人物タブ・履歴タブの検索欄で1文字入力するごとにフォーカスが外れる問題を修正（リスト部分のみ部分更新する方式に変更）

### v2 - UIの細かい修正
- 「説明・メモ」入力欄のラベルとプレースホルダーの高さのズレを修正（上揃えに変更）

### v1 - 初回リリース
- ホーム画面（残高サマリー・最近の記録）
- 人物管理（登録・編集・削除・検索）
- 取引履歴（フィルター・検索・月別グループ表示）
- 取引詳細（返済記録・編集・削除）
- PWA対応（ホーム画面追加・オフライン動作）
- Service Workerによるキャッシュ管理
